[phases.setup]
nixPkgs = ['python310']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
# Skip collectstatic during build - Railway will run it via build.sh if needed
cmds = []

[start]
cmd = 'daphne -b 0.0.0.0 -p $PORT hamsa_ws.asgi:application'
